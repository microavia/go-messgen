// Code generated by go-messgen. DO NOT EDIT.

{{$InternalPrefix := "int_"}}

package {{.Module.Protocol}}

import "math/rand"

var _ = rand.Intn


{{$Module := .}}
{{$Enums := .Enums}}

{{range $Enum := $Enums}}
func {{$InternalPrefix}}{{CamelCase $Enum.Name}}Rand() {{CamelCase $Enum.Name}} {
    return GetAll{{CamelCase $Enum.Name}}()[rand.Intn(len(GetAll{{CamelCase $Enum.Name}}()))]
}

func {{$InternalPrefix}}{{CamelCase $Enum.Name}}SliceRand(l int) ([]{{CamelCase $Enum.Name}}) {
	out := make([]{{CamelCase $Enum.Name}}, l)

	{{$InternalPrefix}}{{CamelCase $Enum.Name}}ArrayRand(out)

	return out
}

func {{$InternalPrefix}}{{CamelCase $Enum.Name}}ArrayRand(out []{{CamelCase $Enum.Name}}) {
	for i := range out {
		out[i] = {{$InternalPrefix}}{{CamelCase $Enum.Name}}Rand()
	}
}

////////////////////////////////////////////////////////////
{{end}}
